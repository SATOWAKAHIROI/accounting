# チケット #009: 品目管理サービス

## ステータス
- [x] 完了

## 機能概要
品目のCRUD操作を提供するサービスクラス。

## 実装ファイル
- [x] `service/ItemService.java`
- [x] `dto/request/ItemRequest.java`（新規作成）
- [x] `dto/response/ItemResponse.java`（新規作成）

## 主要メソッド
- `List<ItemResponse> findAll(Long companyId)` - 全件取得
- `ItemResponse findById(Long id)` - ID検索
- `ItemResponse create(Long companyId, ItemRequest request)` - 作成
- `ItemResponse update(Long id, ItemRequest request)` - 更新
- `void delete(Long id)` - 削除

## ビジネスルール
- 同一会社内でコードが重複しないこと
- 単価は0以上
- 削除時、請求書明細で使用されているかチェック

## 依存関係
- ItemRepository
- InvoiceDetailRepository
